import {getSupabase} from './supabase'
export async function uploadEvidence(file:File,input:{companyId:string;userId:string;missionId:string}){const ext=file.name.split('.').pop()||'jpg',path=`${input.companyId}/${input.missionId}/${input.userId}/${crypto.randomUUID()}.${ext}`;const storage=getSupabase().storage.from('route-evidence');const upload=await storage.upload(path,file,{contentType:file.type||'image/jpeg',upsert:false});if(upload.error)throw upload.error;const signed=await storage.createSignedUrl(path,60*60);if(signed.error)throw signed.error;return{path,url:signed.data.signedUrl}}
