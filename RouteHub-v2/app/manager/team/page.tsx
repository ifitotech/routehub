'use client'
import Link from 'next/link';import {useState} from 'react'
type Member={id:number;name:string;email:string;role:string;active:boolean}
const initial:Member[]=[{id:1,name:'Driver test',email:'driver@example.com',role:'Driver',active:true},{id:2,name:'Operations test',email:'operations@example.com',role:'Operations',active:true}]
export default function Team(){const[members,setMembers]=useState(initial),[open,setOpen]=useState(false),[form,setForm]=useState({name:'',email:'',role:'Driver'});const save=()=>{if(!form.name.trim()||!form.email.trim())return;setMembers([...members,{...form,id:Date.now(),active:true}]);setForm({name:'',email:'',role:'Driver'});setOpen(false)};return <main className="app"><header className="topbar"><Link className="brand" href="/manager">ROUTEHUB</Link><button className="primary" onClick={()=>setOpen(true)}>Add member</button></header><p className="muted">Manager · Team</p><h1>Team members</h1><p className="muted">Invite and manage the people who operate your branch.</p><section style={{display:'grid',gap:12,marginTop:22}}>{members.map(m=><article className="card" key={m.id}><h2>{m.name}</h2><p className="muted">{m.email} · {m.role}</p><div className="actions"><button className="secondary" onClick={()=>setMembers(members.map(x=>x.id===m.id?{...x,active:!x.active}:x))}>{m.active?'Deactivate':'Activate'}</button><button className="secondary" onClick={()=>setMembers(members.filter(x=>x.id!==m.id))}>Remove</button></div></article>)}</section>{open&&<section className="card" style={{marginTop:20}}><h2>Add team member</h2><input placeholder="Full name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input placeholder="Email address" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><select value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option>Driver</option><option>Operations</option><option>Sales</option><option>Counter Sales</option></select><div className="actions"><button className="primary" onClick={save}>Save member</button><button className="secondary" onClick={()=>setOpen(false)}>Cancel</button></div></section>}</main>}
