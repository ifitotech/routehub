'use client'
import {useState} from 'react'
import Link from 'next/link'
import DriverV3Shell from '../../../components/driver-v3/DriverV3Shell'
import {useDriverData} from '../../../lib/driver-v3/use-driver-data'
import {reportIssue} from '../../../lib/driver-v3/actions'
export default function Issue(){const {driverId,snapshot,refresh}=useDriverData();const [note,setNote]=useState('');const [busy,setBusy]=useState(false);const [message,setMessage]=useState('');const route=snapshot?.currentOperation?.route as any;const submit=async()=>{if(!route||busy)return;setBusy(true);setMessage('');try{await reportIssue({routeId:route.id,driverId,companyId:route.company_id},note);await refresh();setMessage('Issue submitted.')}catch(e){setMessage(e instanceof Error?e.message:'Unable to submit issue.')}finally{setBusy(false)}};return <DriverV3Shell active="route"><Link href="/driver-v3/stop" className="muted">‹ Stop details</Link><p className="eyebrow">ROUTE ISSUE</p><h1 className="title">Report an issue</h1><section className="card">{route?<><p className="muted">{route.destination_name||route.destination_address}</p><textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="Describe what happened"/><button className="primary" disabled={busy||!note.trim()} onClick={()=>void submit()}>{busy?'Submitting…':'SUBMIT ISSUE'}</button>{message&&<p role="status" className="muted">{message}</p>}</>:<p className="muted">No current stop.</p>}</section></DriverV3Shell>}
