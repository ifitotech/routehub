'use client'
import {useState} from 'react'
import Link from 'next/link'
import {getSupabase} from '../../lib/supabase'
export default function ForgotPassword(){const[email,setEmail]=useState('');const[message,setMessage]=useState('');const[busy,setBusy]=useState(false);const send=async()=>{if(!email.trim()||busy)return;setBusy(true);setMessage('Sending reset email…');const{error}=await getSupabase().auth.resetPasswordForEmail(email.trim().toLowerCase(),{redirectTo:`${window.location.origin}/login`});setMessage(error?.message||'Check your email for a secure password reset link.');setBusy(false)};return <main className="app"><section className="card" style={{maxWidth:480,margin:'80px auto'}}><h1>Reset your password</h1><p className="muted">Enter your RouteHub email and we&apos;ll send you a secure reset link.</p><input type="email" autoComplete="email" placeholder="you@company.com" value={email} onChange={e=>setEmail(e.target.value)} style={{width:'100%',minHeight:48,margin:'18px 0',padding:'0 14px'}}/><button className="primary" disabled={!email||busy} onClick={()=>void send()}>{busy?'Sending…':'Send reset link'}</button>{message&&<p role="status" className="muted">{message}</p>}<Link href="/login">Back to sign in</Link></section></main>}
