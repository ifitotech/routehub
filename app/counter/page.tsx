'use client'
import {ClipboardList,Contact,Plus,Route,Settings} from 'lucide-react'
const links=[['/contacts','Contactos','Ver y agregar clientes o proveedores',Contact],['/requests','Solicitudes','Crear pickups y deliveries',ClipboardList],['/routes','Rutas','Consultar rutas asignadas',Route],['/settings/contact','Settings','Preferencias y soporte',Settings]] as const
export default function CounterPage(){return <main className="shell counter-page"><div className="brand">RouteHub</div><h1>Counter Sales</h1><p className="muted">Contactos y solicitudes de tu sucursal.</p><button className="primary counter-main-action" onClick={()=>location.href='/requests'}><Plus size={19}/>Nueva solicitud</button><section className="counter-grid">{links.map(([href,title,description,Icon])=><a className="card counter-link" href={href} key={href}><span className="manager-icon"><Icon size={22}/></span><span><strong>{title}</strong><small>{description}</small></span><span className="manager-arrow">›</span></a>)}</section></main>}
