'use client'
import {useEffect,useState} from 'react'
import {completeStop,getCurrentStop} from '../../lib/data'
import {getCurrentLocation} from '../../lib/location'
import {uploadEvidence} from '../../lib/storage'
export default function DriverPage(){const[stop,setStop]=useState<any>(null),[started,setStarted]=useState(false),[photo,setPhoto]=useState<File|null>(null),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);const load=async()=>{try{const r=await getCurrentStop();if(r.error)throw r.error;setStop(r.data||null)}catch{setMessage('No se pudo cargar la ruta o no tienes permisos.')}};useEffect(()=>{load()},[]);const finish=async(method:string,status='completed')=>{if(!stop||busy)return;setBusy(true);try{let loc:any;if(method==='gps')loc=await getCurrentLocation();if(method==='photo_override'&&!photo)throw new Error('Selecciona una foto antes de continuar.');const url=photo?await uploadEvidence(photo,`stops/${stop.id}/${Date.now()}-${photo.name}`):undefined;const r=await completeStop(stop.id,{status,completion_method:method,completion_lat:loc?.lat,completion_lng:loc?.lng,completion_photo_url:url});if(r.error)throw r.error;setMessage(status==='issue'?'Problema guardado.':'Parada completada.');setPhoto(null);await load()}catch(e){setMessage(e instanceof Error?e.message:'No se pudo guardar.')}finally{setBusy(false)}};const address=stop?.contacts?.address||stop?.address||stop?.notes;const mapsUrl=address?`https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(address)}`:'https://www.google.com/maps';return <main className="shell"><div className="eyebrow">ROUTEHUB · DRIVER</div><h1>{started?'Parada actual':'Ruta de hoy'}</h1>{!started?<button className="primary" onClick={()=>setStarted(true)}>COMENZAR RUTA</button>:stop?<article className="card"><span className="label">{stop.type}</span><h2>{stop.notes||'Parada asignada'}</h2><p className="muted">{address}</p><div className="quick"><a className="primary" href={mapsUrl} target="_blank" rel="noreferrer">NAVEGAR</a><button className="primary" disabled={busy} onClick={()=>finish('gps')}>COMPLETAR CON GPS</button></div><input type="file" accept="image/*" capture="environment" onChange={e=>setPhoto(e.target.files?.[0]||null)}/><button className="primary" disabled={!photo||busy} onClick={()=>finish('photo_override')}>COMPLETAR CON FOTO</button><button className="assign" disabled={busy} onClick={()=>finish('photo_override','issue')}>LOCAL CERRADO / REPORTAR</button></article>:<p className="muted">No hay paradas pendientes.</p>}{message&&<p className="label" role="status">{message}</p>}</main>}
