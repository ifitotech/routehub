'use client'
import {useEffect,useState} from 'react'
import {useRouter} from 'next/navigation'
import {getSupabase} from '../../../lib/supabase'
export default function Callback(){const router=useRouter();const [message,setMessage]=useState('Verificando acceso...');useEffect(()=>{(async()=>{try{const supabase=getSupabase();const code=new URLSearchParams(window.location.search).get('code');if(code)await supabase.auth.exchangeCodeForSession(code);const {data}=await supabase.auth.getUser();if(!data.user){setMessage('No se pudo verificar la sesión.');return}const email=data.user.email||'';const {data:approval}=await supabase.from('platform_manager_approvals').select('id').eq('email',email).is('revoked_at',null).maybeSingle();const {data:invite}=await supabase.from('invitations').select('id').eq('email',email).is('accepted_at',null).is('revoked_at',null).gt('expires_at',new Date().toISOString()).maybeSingle();if(!approval&&!invite){setMessage('Este correo no tiene una invitación o autorización activa.');await supabase.auth.signOut();return}router.replace('/') }catch{setMessage('No se pudo verificar el acceso. Revisa la configuración de Supabase.')}})()},[router]);return <main className="shell login"><div className="eyebrow">ROUTEHUB</div><h1>{message}</h1><p className="muted">El acceso requiere autorización de la plataforma o invitación de un Manager.</p></main>}
