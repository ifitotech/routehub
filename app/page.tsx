'use client'
import {useEffect,useState} from 'react'
import {useRouter} from 'next/navigation'
import {Activity,AlertTriangle,ArrowRight,ClipboardList,Clock3,Plus,Truck,Users} from 'lucide-react'
import {getMembership} from '../lib/data'
type Summary={active:number;pending:number;issues:number;drivers:number;recent:any[]}
export default function Home(){const router=useRouter();const[summary,setSummary]=useState<Summary>({active:0,pending:0,issues:0,drivers:0,recent:[]});const[message,setMessage]=useState('');useEffect(()=>{(async()=>{try{const{client,membership}=await getMembership();const[routes,requests,drivers,issues,activity]=await Promise.all([client.from('routes').select('id,status').eq('company_id',membership.company_id),client.from('requests').select('id,status').eq('company_id',membership.company_id),client.from('company_users').select('user_id,role').eq('company_id',membership.company_id).eq('role','driver'),client.from('route_stops').select('id,status,route_id').eq('status','issue'),client.from('activity_logs').select('action,created_at').eq('company_id',membership.company_id).order('created_at',{ascending:false}).limit(5)]);const ids=new Set((routes.data||[]).map((r:any)=>r.id));setSummary({active:(routes.data||[]).filter((r:any)=>r.status==='active').length,pending:(requests.data||[]).filter((r:any)=>['pending','open'].includes(r.status)).length,issues:(issues.data||[]).filter((s:any)=>ids.has(s.route_id)).length,drivers:(drivers.data||[]).length,recent:activity.data||[]})}catch{setMessage('Inicia sesión para cargar el resumen operativo.')}})()},[]);const cards=[{label:'Rutas activas',value:summary.active,icon:<Truck size={21}/>,tone:'blue',href:'/routes/manage'},{label:'Solicitudes pendientes',value:summary.pending,icon:<ClipboardList size={21}/>,tone:'orange',href:'/routes'},{label:'Incidencias abiertas',value:summary.issues,icon:<AlertTriangle size={21}/>,tone:'red',href:'/reports'},{label:'Drivers disponibles',value:summary.drivers,icon:<Users size={21}/>,tone:'green',href:'/admin'}];return <main className="shell dashboard"><header className="topbar"><div><div className="brand">RouteHub</div><h1>Resumen operativo</h1><p className="muted">Todo lo importante de tu operación en un solo lugar.</p></div><div className="avatar">RH</div></header><section className="stats dashboard-stats">{cards.map(c=><button className="stat-card" key={c.label} onClick={()=>router.push(c.href)}><span className={`stat-icon ${c.tone}`}>{c.icon}</span><small>{c.label}</small><strong>{c.value}</strong><span className="stat-link">Ver sección <ArrowRight size={14}/></span></button>)}</section><section className="dashboard-grid"><article className="card dashboard-panel"><div className="section-head"><div><h2>Acciones rápidas</h2><p className="muted">Atajos para el trabajo diario</p></div></div><div className="quick-actions"><button className="primary" onClick={()=>router.push('/routes')}><Plus size={18}/>Crear ruta</button><button className="assign" onClick={()=>router.push('/routes/manage')}><Activity size={18}/>Gestionar despacho</button><button className="assign" onClick={()=>router.push('/reports')}><ClipboardList size={18}/>Abrir reportes</button></div></article><article className="card dashboard-panel"><div className="section-head"><div><h2>Actividad reciente</h2><p className="muted">Últimos cambios registrados</p></div><Clock3 size={19}/></div>{summary.recent.length?summary.recent.map((a:any,i:number)=><div className="activity-row" key={`${a.created_at}-${i}`}><span className="activity-dot"/><div><b>{String(a.action||'Actualización').replaceAll('_',' ')}</b><small>{a.created_at?new Date(a.created_at).toLocaleString():'Ahora'}</small></div></div>):<p className="muted">Aún no hay actividad reciente.</p>}</article></section><section className="card dashboard-tip"><strong>¿Buscas una ruta específica?</strong><span>La lista completa, el orden y la asignación están en Rutas.</span><button className="link-button" onClick={()=>router.push('/routes/manage')}>Ir a rutas <ArrowRight size={16}/></button></section>{message&&<p className="action-feedback feedback-info" role="status">{message}</p>}<footer><span>RouteHub</span><span>v0.1</span></footer></main>}
