'use client'
import {useEffect,useState} from 'react'
import {ArrowUp,ArrowDown,MoreHorizontal,RefreshCw,Pause,Play,AlertTriangle} from 'lucide-react'
import {getMembership} from '../../../lib/data'
import {reorderMissions,canReorder} from '../../../lib/route-planner'
import {ConfirmationModal} from '../../ui'
export default function ManageRoutes(){const[routes,setRoutes]=useState<any[]>([]),[drivers,setDrivers]=useState<any[]>([]),[filter,setFilter]=useState('all'),[msg,setMsg]=useState(''),[loading,setLoading]=useState(true),[confirm,setConfirm]=useState<any>(null);const load=async()=>{setLoading(true);try{const{client,membership}=await getMembership();const rr=await client.from('routes').select('*, route_stops(*)').eq('company_id',membership.company_id).order('route_date',{ascending:false}).order('position',{ascending:true});if(rr.error)throw rr.error;const dd=await client.from('company_users').select('user_id,role,users(email,name)').eq('company_id',membership.company_id);if(dd.error)throw dd.error;setRoutes(rr.data||[]);setDrivers(dd.data||[])}catch(e){setMsg(e instanceof Error?e.message:'Could not load routes.')}finally{setLoading(false)}};useEffect(()=>{load()},[]);const driverLabel=(id:string)=>{const d=drivers.find(x=>x.user_id===id);return d?.users?.name||d?.users?.email||'Team member'};const update=async(id:string,patch:any,action='route_updated')=>{try{const{client,user,membership}=await getMembership();const{error}=await client.from('routes').update({...patch,updated_version:Date.now()}).eq('id',id);if(error)throw error;await client.from('activity_logs').insert({company_id:membership.company_id,user_id:user.id,action,record_id:id,after_value:patch});setRoutes(rs=>rs.map(r=>r.id===id?{...r,...patch}:r));setMsg('Change saved.')}catch(e){setMsg(e instanceof Error?e.message:'Could not update.')}};const move=async(index:number,delta:number)=>{const shown=filtered;const target=index+delta;if(target<0||target>=shown.length||!canReorder(shown[index]))return;const ordered=reorderMissions(shown.map((r,i)=>({id:r.id,position:i+1,status:r.status,type:r.route_stops?.[0]?.type||'delivery',destination_address:r.destination_address,destination_name:r.destination_name,origin_name:r.origin_name})),index,target);for(const m of ordered)await update(m.id,{position:m.position},'route_reordered');await load()};const filtered=filter==='all'?routes:routes.filter(r=>r.driver_id===filter);return <main className="shell dispatch-page"><div className="eyebrow">ROUTEHUB  DISPATCH</div><div className="dispatch-title"><div><h1>Missions by driver</h1><p className="muted">Organize operations live.</p></div><button className="secondary" onClick={load}><RefreshCw size={17}/>Reload</button></div><select className="dispatch-filter" value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">All drivers</option>{drivers.map(d=><option value={d.user_id} key={d.user_id}>{driverLabel(d.user_id)} Â· {String(d.role||'team member').replaceAll('_',' ')}</option>)}</select>{loading&&<div className="loading-skeleton"/>}{!loading&&filtered.map((r,i)=><article className={`dispatch-card ${r.priority==='urgent'?'urgent':''}`} key={r.id}><div className="timeline-dot"/><div className="dispatch-content"><div className="dispatch-card-head"><span className="status-badge status-info">Route {i+1} · {r.status}</span>{r.priority==='urgent'&&<span className="status-badge status-danger"><AlertTriangle size={14}/> URGENTE</span>}<button className="icon-button" aria-label="More actions"><MoreHorizontal/></button></div><h2>{r.destination_name||r.destination_address||r.route_date}</h2><p className="muted">{r.origin_name||r.origin_address||'Origin pending'}  {r.destination_address||'Destination pending'}</p><p className="muted"><b>Driver:</b> {driverLabel(r.driver_id)}  <b>PO:</b> {r.order_number||''}  {r.scheduled_at?new Date(r.scheduled_at).toLocaleTimeString([], {hour:'numeric',minute:'2-digit'}):'No time'}</p><div className="dispatch-actions"><select value={r.driver_id||''} onChange={e=>update(r.id,{driver_id:e.target.value},'route_reassigned')}>{drivers.map(d=><option key={d.user_id} value={d.user_id}>{driverLabel(d.user_id)} Â· {String(d.role||'team member').replaceAll('_',' ')}</option>)}</select><select value={r.status} onChange={e=>{const next=e.target.value;if(next==='paused'||next==='cancelled')setConfirm({id:r.id,status:next});else update(r.id,{status:next},'route_status_changed')}}><option value="draft">Draft</option><option value="published">Published</option><option value="active">Active</option><option value="paused">Paused</option><option value="completed">Completed</option><option value="cancelled">Cancelled</option></select><button className="assign" disabled={!canReorder(r)} onClick={()=>move(i,-1)}><ArrowUp size={15}/>Move up</button><button className="assign" disabled={!canReorder(r)} onClick={()=>move(i,1)}><ArrowDown size={15}/>Move down</button><button className="assign" onClick={()=>update(r.id,{priority:'urgent'},'route_marked_urgent')}><AlertTriangle size={15}/>Urgente</button></div></div></article>)}{!loading&&!filtered.length&&<p className="muted">No missions for this team member.</p>}{msg&&<p className="action-feedback feedback-info" role="status">{msg}</p>}<ConfirmationModal open={!!confirm} title={confirm?.status==='paused'?'Pause mission':'Cancel mission'} description={confirm?.status==='paused'?'The team member will pause this mission and can resume it later.':'The mission will be cancelled and its history will be preserved.'} confirmLabel={confirm?.status==='paused'?'Pause':'Cancel mission'} onCancel={()=>setConfirm(null)} onConfirm={async()=>{if(confirm){await update(confirm.id,{status:confirm.status},'route_status_changed');setConfirm(null)}}}/></main>}
