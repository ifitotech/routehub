'use client'
import {useEffect,useState} from 'react'
import {getMembership} from '../../lib/data'
type Mission={type:'pickup'|'delivery'|'transfer'|'return_to_branch';driver:string;origin:string;originName:string;destination:string;destinationName:string;name:string;contact_id?:string;po:string;notes:string;priority:'normal'|'priority'|'urgent';date:string;time:string}
const typeLabels={pickup:'Recogida',delivery:'Entrega',transfer:'Transferencia',return_to_branch:'Regresar a sucursal'}
export default function Routes(){const today=new Date().toISOString().slice(0,10);const[m,setM]=useState<Mission>({type:'delivery',driver:'',origin:'',originName:'branch',destination:'',destinationName:'',name:'',po:'',notes:'',priority:'normal',date:today,time:''});const[list,setList]=useState<Mission[]>([]);const[contacts,setContacts]=useState<any[]>([]),[drivers,setDrivers]=useState<any[]>([]),[message,setMessage]=useState('');useEffect(()=>{(async()=>{try{const{client,membership,user}=await getMembership();setContacts((await client.from('contacts').select('*').eq('company_id',membership.company_id)).data||[]);const d=(await client.from('company_users').select('user_id,role,users(email,name)').eq('company_id',membership.company_id).in('role',['driver','branch_manager','operations_manager'])).data||[];setDrivers(d);if(user?.id)setM(x=>({...x,driver:user.id}));else if(d[0])setM(x=>({...x,driver:d[0].user_id}))}catch{setMessage('No se pudo cargar la empresa.')}})()},[]);const add=()=>{if(!m.driver||!m.destination&&!m.name){setMessage('Selecciona conductor y destino.');return}const c=contacts.find(x=>x.company_name===m.name);setList(x=>[...x,{...m,destination:m.destination||c?.address||'',destinationName:m.destinationName||m.name,contact_id:c?.id}]);setMessage('Ruta aÃƒÂ±adida a la lista del dÃƒÂ­a.');setM(x=>({...x,destination:'',destinationName:'',name:'',po:'',notes:''}))};const save=async(publish=false)=>{try{const{client,membership,user}=await getMembership();for(let i=0;i<list.length;i++){const x=list[i];const{data:r,error}=await client.from('routes').insert({company_id:membership.company_id,branch_id:membership.branch_id,driver_id:x.driver,route_date:x.date,scheduled_at:x.time?`${x.date}T${x.time}:00`:null,origin_address:x.origin,origin_name:x.originName,destination_address:x.destination,destination_name:x.destinationName,order_number:x.po,priority:x.priority,status:publish?'published':'draft'}).select().single();if(error||!r)throw error;const s=await client.from('route_stops').insert({route_id:r.id,contact_id:x.contact_id||null,type:x.type==='return_to_branch'?'return':x.type,position:1,status:'pending',po:x.po,notes:x.notes,origin_address:x.origin,destination_address:x.destination,priority:x.priority,scheduled_at:x.time?`${x.date}T${x.time}:00`:null});if(s.error)throw s.error;await client.from('activity_logs').insert({company_id:membership.company_id,user_id:user.id,action:'mission_created',record_id:r.id,after_value:{position:i+1,priority:x.priority}})}setList([]);setMessage(publish?'Rutas publicadas.':'Borradores guardados.')}catch(e){setMessage(e instanceof Error?e.message:'No se pudieron guardar las rutas.')}};const driverLabel=(id:string)=>{const d=drivers.find(x=>x.user_id===id);return d?.users?.name||d?.users?.email||id};return <main className="shell"><div className="eyebrow">ROUTEHUB</div><h1>AÃƒÂ±adir ruta</h1><p className="muted">Cada recogida o entrega es una misiÃƒÂ³n independiente.</p><section className="card form"><label>Driver<select value={m.driver} onChange={e=>setM({...m,driver:e.target.value})}>{drivers.map(d=><option key={d.user_id} value={d.user_id}>{driverLabel(d.user_id)}</option>)}</select></label><label>Tipo de ruta<select value={m.type} onChange={e=>setM({...m,type:e.target.value as Mission['type']})}>{Object.entries(typeLabels).map(([v,l])=><option value={v} key={v}>{l}</option>)}</select></label><label>Punto de salida<select value={m.originName} onChange={e=>setM({...m,originName:e.target.value})}><option value="branch">Sucursal</option><option value="manual">DirecciÃƒÂ³n manual</option><option value="previous_route">Final de ruta anterior</option><option value="driver_location">UbicaciÃƒÂ³n actual del driver</option></select></label>{m.originName==='manual'&&<label>DirecciÃƒÂ³n de salida<input value={m.origin} onChange={e=>setM({...m,origin:e.target.value})}/></label>}<label>Destino / contacto<input list="route-contacts" value={m.name} onChange={e=>{const v=e.target.value,c=contacts.find(x=>x.company_name===v);setM({...m,name:v,destination:c?.address||m.destination,destinationName:c?.company_name||v})}} placeholder="Seleccionar contacto"/><datalist id="route-contacts">{contacts.map(c=><option key={c.id}>{c.company_name}</option>)}</datalist></label><label>DirecciÃƒÂ³n de destino<input value={m.destination} onChange={e=>setM({...m,destination:e.target.value})} placeholder="DirecciÃƒÂ³n manual"/></label><div className="form-grid"><label>PO / Referencia<input value={m.po} onChange={e=>setM({...m,po:e.target.value})}/></label><label>Prioridad<select value={m.priority} onChange={e=>setM({...m,priority:e.target.value as Mission['priority']})}><option value="normal">Normal</option><option value="priority">Prioridad</option><option value="urgent">Urgente</option></select></label><label>Fecha<input type="date" value={m.date} onChange={e=>setM({...m,date:e.target.value})}/></label><label>Hora programada<input type="time" value={m.time} onChange={e=>setM({...m,time:e.target.value})}/></label></div><label>Notas e instrucciones<textarea rows={4} value={m.notes} onChange={e=>setM({...m,notes:e.target.value})} placeholder="Instrucciones visibles para el driver"/></label><button className="primary" onClick={add}>+ AÃƒÂ±adir ruta</button></section><h2>Rutas del dÃƒÂ­a ({list.length})</h2>{list.map((x,i)=><article className={`card mission-card ${x.priority==='urgent'?'urgent':''}`} key={`${x.date}-${i}`}><div><span className="label">Ruta {i+1} Ã‚Â· {typeLabels[x.type]} {x.priority==='urgent'&&' Ã‚Â· URGENTE'}</span><h3>{x.originName==='previous_route'?'Final de la ruta anterior':x.origin||'Sucursal'} Ã¢â€ â€™ {x.destinationName||x.destination}</h3><p className="muted">{driverLabel(x.driver)} Ã‚Â· {x.time||'Sin hora'} Ã‚Â· PO: {x.po||'Ã¢â‚¬â€'}</p><p>{x.notes||'Sin instrucciones adicionales.'}</p></div><button className="assign" onClick={()=>setList(list.filter((_,j)=>j!==i))}>Quitar</button></article>)}<div className="quick"><button className="primary" disabled={!list.length} onClick={()=>save(false)}>Guardar borradores</button><button className="primary" disabled={!list.length} onClick={()=>save(true)}>Publicar rutas</button></div>{message&&<p className="label">{message}</p>}</main>}
