import {getSupabase} from './supabase'
export async function uploadEvidence(file:File,path:string){const s=getSupabase();const{data,error}=await s.storage.from('routehub-evidence').upload(path,file,{upsert:true,contentType:file.type});if(error)throw error;const signed=await s.storage.from('routehub-evidence').createSignedUrl(data.path,60*60);if(signed.error)throw signed.error;return signed.data.signedUrl}
